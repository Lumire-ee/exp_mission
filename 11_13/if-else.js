const a = true;
const b = false;

if (a) {
  console.log("이 조건은 true입니다.");
} else {
  console.log("이 조건은 false입니다.");
}

const ternaryOperator = b ? "이 조건은 true입니다." : "이 조건은 false입니다.";

console.log(ternaryOperator);
